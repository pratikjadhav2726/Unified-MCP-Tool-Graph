# MCP Unified Gateway Configuration
# Copy this file to .env and fill in your actual values

# Neo4j Configuration (Optional - system will fallback to "everything" server if not provided)
# If you have a Neo4j database for dynamic tool retrieval, configure these:
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_neo4j_password

# API Keys for MCP Servers (Optional - only needed if using specific servers)
# Tavily Search API (for web search capabilities)
TAVILY_API_KEY=your_tavily_api_key_here

# OpenAI API Key (if using OpenAI-based servers)
OPENAI_API_KEY=your_openai_api_key_here

# Groq API Key (if using Groq-based servers)
GROQ_API_KEY=your_groq_api_key_here

# Other API keys can be added here as needed
# ANTHROPIC_API_KEY=your_anthropic_api_key_here
# GOOGLE_API_KEY=your_google_api_key_here

# Gateway Configuration
GATEWAY_PORT=8000
PROXY_PORT=9000

# Logging Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO